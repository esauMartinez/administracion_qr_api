services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: administracion-qr-app
    ports:
      - '5000:5000'
    env_file:
      - .env
    restart: unless-stopped
